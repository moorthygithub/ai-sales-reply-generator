import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "AI Sales Reply Generator",
  description:
    "Generate professional sales email replies instantly using AI. Paste a customer email, select a tone, and get a polished response in seconds.",
  keywords: [
    "AI email reply",
    "sales email generator",
    "email automation",
    "AI sales tool",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${inter.variable} font-sans antialiased`} suppressHydrationWarning>
        {children}
      </body>
    </html>
  );
}
